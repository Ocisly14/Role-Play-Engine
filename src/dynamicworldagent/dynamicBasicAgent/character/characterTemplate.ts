/**
 * Character Agent Template
 *
 * NPC response analysis template has been removed.
 * That responsibility is now handled by the TickProcessor's impact gate.
 *
 * This file is kept as a placeholder in case future character-related
 * template functionality is needed.
 */
export function getCharacterTemplate(): string {
  return "";
}
