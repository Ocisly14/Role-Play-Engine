在独立开发 LLM 驱动的跑团游戏过程中，我逐渐意识到：跑团的本质，其实是在不断“再造一个世界”。

玩家输入行动，世界根据规则、角色、环境与事件条件发生变化；NPC 根据自己的记忆、关系和目标做出反应；场景、物品、天气、时间和剧情线持续演化。
这让我开始思考：既然跑团本身就是一个动态世界，那么能不能开发一个由 LLM 驱动的游戏引擎？

传统游戏引擎的核心，是在接收到事件或条件变化后，对世界状态进行确定性的更新。
而 LLM 游戏引擎的核心，则可以是：通过写入不同类别的 Skills，指导 LLM 理解并处理不同类型的世界状态变化。

比如：

物体可以被自由拆分、重组和使用；
角色之间可以发生开放式互动；
玩家可以改造、破坏，甚至局部重塑场景；
不同职业、工具、环境和条件，会影响同一个行动的结果。

基于这个想法，我完成了最初版本的 LLM World Engine。

它的设计有点类似 Claude Code 的 Skill 机制：将玩家或 NPC 的行动拆分成不同类别，例如电器维修、驾驶、潜水、攀爬、调查、社交、战斗等。
每一类 Skill 都会定义：

这个行动需要读取哪些世界状态；
行动过程中需要参考哪些规则；
它可能如何改变角色、物品、环境或关系；
最终结果应该如何写回世界状态。

在执行时，系统会按需注入相关状态，由 LLM 生成结果，再将变化更新回世界模型中。

但随着开发深入，LLM 的局限也很快显现出来：
速度太慢，成本太高，且并不是所有变化都需要 LLM 处理。

于是我引入了传统引擎，与 LLM 引擎协同工作，并新增了一个任务处理器。

任务处理器会将玩家或 NPC 输入的多步骤行动，拆解成原子化的处理流程，再根据任务类型分配给不同引擎：

简单移动、时间推进、全局天气变化、数值结算等确定性任务，由传统引擎处理；
物品拆解、场景互动、NPC 社交、复杂推理等开放性任务，由 LLM 引擎处理；
概率、规则和矩阵可以交给 code；
叙事、语义和开放结果则交给 LLM。

通过处理队列，传统引擎与 LLM 引擎可以无缝衔接：
一个负责确定性，一个负责开放性；
一个提供效率，一个提供想象力。

有了 LLM 世界引擎，接下来的问题是：
角色该如何真正与这个世界交互？

在开发跑团 NPC 的过程中，我找到了一个方向。

LLM 本身像一个空白的超级大脑。
当我们赋予它姓名、年龄、职业、经历、性格、目标和秘密，它就不再只是一个文本生成器，而开始像一个“角色”。

但真正塑造一个人行为的，不只是设定，而是记忆。

因此，我为 NPC 引入了短期记忆、长期记忆、路径地图、角色关系网络，以及遗忘曲线。
同时给它们提供写入记忆、回忆记忆、总结记忆的 tools。

当然，无论是 grep 还是 RAG，记忆的检索都存在不确定性。
但这反而很像真实的人：
人本身也是会遗忘、误解、选择性回忆的。

当 NPC 有了行动能力和记忆能力，新的问题又出现了：

NPC 如何感知这个世界？

于是我引入了“渲染层”。

它类似传统游戏引擎中的图像渲染，只不过这里渲染的不是画面，而是文字化的环境描述。
渲染层会读取 NPC 周围的世界状态，并将其转换成 LLM 能理解的文字输入。

这相当于 NPC 的“摄像头”。

NPC 通过渲染层“看到”周围的场景：
房间里有什么人；
哪些物体可交互；
天气和光线如何；
是否有危险；
其他角色的动作和情绪如何。

如果未来结合成熟的 3D 或 2D 游戏引擎，渲染层甚至可以直接输出真实画面，再将视觉信息一同注入 prompt 中，让 NPC 通过多模态方式感知世界。

于是，一个完整的循环便形成了：

世界状态被渲染成 NPC 可感知的信息；
NPC 根据自身设定、记忆和目标做出行动；
任务处理器将行动拆解成原子任务；
传统引擎和 LLM 引擎分别处理；
结果被写回世界状态；
世界进入下一次变化。

不过，出于成本和效率的考虑，这个世界不能无限实时运转。
它需要 tick。

在每一个 tick 中，世界发生一次状态更新。
这个 tick 可以是游戏内的 1 分钟，也可以是 5 分钟，甚至更长。

不妨把 tick 理解为 LLM 世界引擎的“帧率”。

传统游戏的帧率取决于图形渲染速度；
LLM 世界的帧率，则取决于状态处理速度、推理速度和 token 输出速度。

两类引擎输出结果越快，每个 tick 的间隔就可以越短，世界变化也就越流畅。

NVIDIA 最初推动 GPU 发展，是为了更快地绘制三角形、渲染图像。
而在 LLM 游戏引擎中，token 的生成速率，也许会成为另一种意义上的“渲染资源”。

过去，显卡渲染的是图像世界；
现在，它也许可以渲染一个由语言、记忆、行动和状态共同构成的动态世界。

让显卡重新回到游戏的怀抱。

Make Games Great Again.